// module.exports = app => {
//     const user = require("../controllers/controllers");
//     var router = require("express").Router();

//     app.use("/api/users", router);

    //Get users.
    // router.get("/all", user.getUsers);

    //Get a single user.
//     router.get("/one/:id", user.findOne);
// }